{
  "buildCommand": "cd novacal-frontend && npm install && npm run build",
  "outputDirectory": "novacal-frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
